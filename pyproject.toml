[project]
name = "template-scraping"
version = "0.1.0"
description = "trying to create a template for scraping"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "loguru>=0.7.3",
    "pre-commit>=4.5.1",
    "ruff>=0.15.0",
    "selectolax>=0.4.6",
]
